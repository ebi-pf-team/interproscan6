process {
    withLabel:use_gpu {
        containerOptions = '--gpus all'
    }
}

docker {
    enabled = true
    mountFlags = 'Z'
    pullTimeout = '3 hours'  // the default is 20 minutes and fails with large images
}
